# Run MSCONVERT

Install and run `msconvert` for the greater good on Linux and perhaps MacOS.
Prerequisites: working `docker`.

```
Usage: msconvert <input.mgf> <output_folder>
```

E.g. `msconvert example/test.mgf example/out` should make `example/out/test.mzML`.


# LICENCE 
Check the [original source](https://hub.docker.com/r/proteowizard/pwiz-skyline-i-agree-to-the-vendor-licenses).
